# cargo-release config
[workspace]
# sokm must publish before sokm-kernel (dependency order)
publish-order = [
  "sokm",
  "sokm-kernel",
]

[publish]
verify = true

[tag]
# Tag format: v0.1.0
name = "v{{version}}"

[push]
push = true
